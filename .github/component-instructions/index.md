### instruction

Given a state variable and its type definition, follow the instructions defined
first inside state.md, then actions.md, dispatches.md, reducers.md, and
test-vitest.md. Generate the necessary code inside the respective files. Ensure
that if the files already contain code, you add the provided state variable to
the existing code while maintaining consistency with the existing structure and
conventions.
